# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# The home page is the "sign in" one.
GET     /			controllers.HomeController.signin

GET     /signup.html		controllers.HomeController.signup

GET     /form_register.html		controllers.HomeController.form_register

# Show the home page of the user with the given id.
GET     /users/:id		controllers.Users.index(id: Long)

# Display all the existing submissions.
GET	/submissions		controllers.Submissions.all

# Map static resources from the /public folder to the /assets URL path.
GET     /assets/*file           controllers.Assets.versioned(path = "/public", file: Asset)
